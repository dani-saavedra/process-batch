spring:
  application:
    name: resired
  datasource:
    password: ${DB_PASS}
    username: ${DB_USER}
    url: jdbc:mysql://${DB_HOST}:${DB_PORT}/${DB_NAME}
  jpa:
    hibernate:
      ddl-auto: validate
    database-platform: org.hibernate.dialect.MySQL8Dialect
server:
  servlet:
    context-path: /api
